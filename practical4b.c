#include<stdio.h>
#include<conio.h>
void main()
{
	int main,i,j,k=1;
	printf("enter a number to define the rows in floyd's triangle:\n");
	scanf("%d",&num);
	for(i=1,i<=num:i++)
	{
	
    for(j=1,j<=i:j++)
	{
		printf("%2d",k++);
	}
	printf("\n");
	getch();
}
